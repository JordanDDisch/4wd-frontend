import React from 'react';
import { storiesOf } from '@storybook/react';

// import Routes from './../Routes';

storiesOf('Routes', module).add('Demo', () => {
    return (
        <div>
            Implementing this story will require some investigation into how to
            mock react-router
        </div>
    );

    // return <Routes hasOverlay={false} />;
});
